using System;
using System.Collections.Generic;
using System.Text;

namespace GameOfLife
{
    public enum Status
    {
        Started, Stopped
    }
    public enum Direction
    {
        Up, Down, Left, Right
    }
}
